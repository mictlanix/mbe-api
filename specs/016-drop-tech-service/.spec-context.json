{
  "workflow": "speckit",
  "specName": "drop tech service",
  "branch": "016-drop-tech-service",
  "currentStep": "plan",
  "status": "planned",
  "history": [
    {
      "step": "specify",
      "substep": null,
      "kind": "start",
      "by": "extension",
      "at": "2026-08-30T02:51:33.357Z"
    },
    {
      "step": "specify",
      "substep": null,
      "kind": "complete",
      "by": "extension",
      "at": "2026-08-30T02:54:29.374Z"
    },
    {
      "step": "plan",
      "substep": null,
      "kind": "start",
      "by": "extension",
      "at": "2026-08-30T02:57:55.094Z"
    },
    {
      "step": "plan",
      "substep": "research",
      "kind": "complete",
      "by": "ai",
      "at": "2026-08-30T03:00:55.613Z"
    },
    {
      "step": "plan",
      "substep": "design",
      "kind": "complete",
      "by": "ai",
      "at": "2026-08-30T03:01:47.305Z"
    },
    {
      "step": "plan",
      "substep": null,
      "kind": "complete",
      "by": "extension",
      "at": "2026-08-30T03:02:04.839Z"
    }
  ],
  "telemetryInstanceId": "e39ae22d-c79f-465b-b7af-38361a4eab9f",
  "last_action": "review-plan gate approved with the quiet ordering; mutation proof taken as its own task",
  "coverage": {
    "FR-001": {
      "title": "Must not map any of the seven dropped tables; the mapping module is removed in full"
    },
    "FR-002": {
      "title": "Must remove the four retired menu entries from the catalog it defines"
    },
    "FR-003": {
      "title": "Must not itself delete permission rows; removal happens through existing full-replace"
    },
    "FR-004": {
      "title": "Must not reuse or renumber retired identifiers, nor change surviving ones"
    },
    "FR-005": {
      "title": "Data dictionary must carry no section or pending-removal note for the seven"
    },
    "FR-006": {
      "title": "Documentation completeness check must carry no waiver naming the seven"
    },
    "FR-007": {
      "title": "Live-schema derivation must not include any of the seven tables"
    },
    "FR-008": {
      "title": "Stated permission matrix width must be consistent everywhere it appears"
    },
    "FR-009": {
      "title": "No endpoint behaviour change and no database migration from this repository"
    },
    "FR-010": {
      "title": "Full test suite passes with no reference to the seven outside historical record"
    }
  },
  "size": "normal",
  "classification": {
    "projectedFiles": 12,
    "projectedTasks": 12,
    "scopeSignal": "larger",
    "verdict": "normal"
  },
  "context": [
    "area: app/models/technical_service.py — maps all seven dropped tables, incl. vehicle_service_order and service_order_detail",
    "area: app/enums.py SystemObject — retiring 58/64/65/90 narrows the permission matrix from 107",
    "area: app/services/user_service.py _write_privileges_from — writes one row per enum member and prunes rows outside it",
    "area: docs/mbe_schema.sql + migrations/ — the live-schema derivation read by test_model_schema and test_data_dictionary",
    "constraint: mbe-api adds no migration and deletes no access_privilege rows (mictlanix/mbe#37)",
    "constraint: retired identifiers are left unused, never recycled — the catalog is shared with the legacy system"
  ],
  "intent": "Bring this repository's description of the database back into agreement with the deployment after the monolith dropped seven abandoned tables, so the standing schema checks measure against a true baseline.",
  "expectations": [
    "No database migration is added by this repository",
    "No access_privilege rows are deleted by this repository",
    "No endpoint behaviour changes",
    "The four retired menu-entry identifiers are not reused or renumbered",
    "No export or recovery path for the dropped rows"
  ],
  "approach": "Deletion in four places plus the constant that follows. Delete app/models/technical_service.py and its import, remove SystemObject 58/64/65/90, remove the seven data-dictionary sections and the seven CREATE TABLE blocks from docs/mbe_schema.sql, drop the six waivers, and move the matrix width from 107 to 103. No migration, no cleanup code, no endpoint change.",
  "decisions": [
    {
      "decision": "Remove the seven CREATE TABLE definitions from docs/mbe_schema.sql",
      "why": "Keeps the live-schema derivation as dump+migrations with no third input and no hand-maintained list; both standing checks currently pass against a schema with seven tables the deployment lacks, which is a check that has stopped measuring",
      "rejected": "Recording the drop as a migration here (re-states a change another repo owns, ruled out by the issue, breaks SC-008); an exclusion list in the two checks (a hand-maintained list of exactly the shape #112 was)"
    },
    {
      "decision": "Write no permission-row cleanup code",
      "why": "_write_privileges_from already removes rows naming an object the enum does not define — the loop written for objects 70/104/105. Removing the enum members is simultaneously the fix and the cleanup, and honours the constraint that this repo deletes no privilege rows",
      "rejected": "A one-off cleanup migration or script (nothing to clean: mbe_dev has zero rows for the four); filtering the four at the write site (a special case in the one function that has none)"
    },
    {
      "decision": "Delete the four enum members rather than keeping them unused",
      "why": "Their access_privilege rows and their screens are both gone, so keeping the members would re-create four rows per account forever for menu entries that do not exist",
      "rejected": "Mirroring EXCLUDE_PRICE_RANGE_VALIDATION=102, kept unused by spec 015 — correct there because its rows still exist, wrong here because these were deleted; renumbering to close the gaps (would re-point stored grants keyed on those numbers)"
    },
    {
      "decision": "Delete the models and correct the dump in one task (the quiet order)",
      "why": "Decided at the review-plan gate. A red commit on the branch cannot be bisected through and its on-purpose-ness does not survive the author forgetting; the evidence the loud order bought is taken separately by local mutation instead",
      "rejected": "Correct the dump first and leave the models briefly unmapped — fails loudly and accurately, proving the check is live, but leaves one red commit in history"
    }
  ],
  "step_summaries": {
    "plan": {
      "summary": "12 files, all deletion plus one constant; no migration, no new code path",
      "key_finding": "107 means two different things — the member count (becomes 103) and PRODUCTION_SITES = 107 (unchanged). Identifiers run to 113, so their equality was coincidence, and a careless find-and-replace silently rewrites a test fixture in test_user_profile_service.py:87."
    }
  }
}
